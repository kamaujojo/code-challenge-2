function addItem(grapes) {
    const itemInput = document.getElementById('grapes');
    const itemName = itemInput.value.
    if (grapes === '') return; 
    
    const itemList = document.getElementById('itemList');}

  